# Core library has no hard dependencies. Install what your backend needs:
#   pip install panda3d      # for mcshader.engine.Panda3DAdapter
#   pip install PyOpenGL     # for mcshader.engine.GenericGLAdapter
# Or via extras:  pip install -e ".[panda3d]"   /   pip install -e ".[dev]"
